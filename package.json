{
  "name": "folder-practice",
  "version": "1.0.0",
  "description": "An app that finds out which friends you are most like",
  "main": "server.js",
  "scripts": {
    "start": "nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:create": "createdb Same-NoT-Same < src/models/db/schema/schema.sql",
    "db:delete": "dropdb Same-NoT-Same",
    "db:reset": "dropdb Same-NoT-Same && createdb Same-NoT-Same && npm run load_schema",
    "load_schema": "psql -d Same-NoT-Same < src/models/db/schema/schema.sql"

  },
  "keywords": [],
  "author": "Jose Moreno",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "ejs": "^2.5.7",
    "express": "^4.16.2"
  }
}
